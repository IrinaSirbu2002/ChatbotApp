 <div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">ChatbotApp</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <h4 style="color: white">Choose one of the following topics:</h4>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" Match="NavLinkMatch.All" >
                <span class="oi" aria-hidden="true"></span>Books
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" Match="NavLinkMatch.All">
                <span class="oi" aria-hidden="true"></span>Films
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" Match="NavLinkMatch.All">
                <span class="oi" aria-hidden="true"></span>TV Series
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" Match="NavLinkMatch.All">
                <span class="oi" aria-hidden="true"></span>Poetry
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" Match="NavLinkMatch.All">
                <span class="oi" aria-hidden="true"></span>Paintings
            </NavLink>
        </div>
    </nav>
</div>

@code {
   
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}

